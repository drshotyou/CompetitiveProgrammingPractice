#include <bits/stdc++.h>

using namespace std;

int main () {
	
	int l,r,c;
	while(cin >> l >> r >> c && l!=0 && r!=0 && c!-0)
	{
		for(int i=0;i<l;i++)
		{
			int maze[r][c]={{0}};
			for(int j=0;j<r;j++)
			{
				for(int k=0;k<c;k++)
				{
					char c
				}
			}
		}
	}
	
	return 0;
}
